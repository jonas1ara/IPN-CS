#include <stdio.h> //Directiva(Indica que se incluya en el programa el encabezado estandar de entrada y salida)

int main(void) //Función principal del programa, dentro va el bloque de código que se va a ejecutar.

{
	printf("Hello world\n"); //Imprimir una cadena de caracteres, \n caracter de escape y el ; indica que ha finalizado esa línea de código.

	return 0; //Return0 indica que el programa se termino con exito.
}
