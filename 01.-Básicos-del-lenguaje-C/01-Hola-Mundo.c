/*
	Hola mundo 🤣🤣
*/

#include <stdio.h> //Directiva(Indica que el encabezado de entrada y salida estándar se incluya en el programa)

int main(void) // principal del programa, dentro va el bloque de código a ejecutar y VOID especifica claramente que main puede ser llamado sólo sin ningún parámetro.
{
	printf("Hola Mundo\n"); //Imprimir una cadena de caracteres, n el carácter de escape y el ; indica que se ha completado esa línea de código.

	return 0; //Return 0 indica que el programa finalizó correctamente.
}
